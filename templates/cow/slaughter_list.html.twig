{% extends 'base.html.twig' %}

{% block title %}Lista de Abate{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2 class="text-danger">Lista de Abate</h2>
            <span class="badge bg-danger fs-6">{{ elegiveis|length }} animais</span>
        </div>

        {% if elegiveis|length == 0 %}
            <div class="alert alert-success">
                Nenhum animal pronto para abate
            </div>
        {% else %}
            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead class="table-dark">
                    <tr>
                        <th>Código</th>
                        <th>Fazenda</th>
                        <th>Nascimento / Idade</th>
                        <th>Leite (L/sem)</th>
                        <th>Ração (kg/dia)</th>
                        <th>Peso (@)</th>
                        <th>Motivos</th>
                        <th>Ação</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in elegiveis %}
                        <tr>
                            <td><span class="badge bg-danger">{{ item.cow.codigo }}</span></td>
                            <td>{{ item.cow.fazenda.name }}</td>
                            <td>
                                {{ item.cow.dataNascimento|date('d/m/Y') }}
                                <br><small class="text-muted">{{ item.cow.idadeEmAnos|number_format(1, ',', '.') }} anos</small>
                            </td>
                            <td>{{ item.cow.leiteFloat|number_format(2, ',', '.') }}</td>
                            <td>{{ item.cow.racaoPorDia|number_format(2, ',', '.') }}</td>
                            <td>{{ item.cow.pesoEmArrobas|number_format(2, ',', '.') }}@</td>
                            <td>
                                {% for motivo in item.motivos %}
                                    <span class="badge bg-warning text-dark d-block mb-1">{{ motivo }}</span>
                                {% endfor %}
                            </td>
                            <td>
                                <form method="post" action="{{ path('cow_slaughter', {id: item.cow.id}) }}"
                                      onsubmit="return confirm('Confirma o abate do animal {{ item.cow.codigo }}?')">
                                    <input type="hidden" name="_token" value="{{ csrf_token('abate-' ~ item.cow.id) }}">
                                    <button class="btn btn-sm btn-danger">Abater</button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>
{% endblock %}